import "package:flutter/material.dart";

class add extends StatelessWidget {
  const add({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Hello To  Add Page')),
    );
  }
}
